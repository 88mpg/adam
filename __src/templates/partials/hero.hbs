<header id="hero" class="hero">
  <div class="hero--content">
    <div class="hero--content-inner">
      <h1>{{{ title }}}</h1>
      <h3>{{{ description }}}</h3>
      {{#each copy}}
      <p>{{{ this }}}</p>
      {{/each}}
    </div>
  </div>
  <div class="scroll" id="goDown"><a class="more" href="#">&#65516;</a></div>
</header>
